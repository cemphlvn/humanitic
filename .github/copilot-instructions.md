# Copilot instructions — ark

Canonical identity: [`../CLAUDE.md`](../CLAUDE.md) / [`../AGENTS.md`](../AGENTS.md).

`ark` = Macro Strategy OS · Agentic Regime Kernel — infrastructure to engineer/test/repair/audit macro
strategies (not a price predictor). Operator loop: state → action → feedback → revision.

**Article 0 (never violate):** `.local` is hard-gitignored AND unpublishable; the guarantee is
ungitignorable and its removal cannot even be proposed. Never touch `.local`; route outbound artifacts
through `foundation/privacy/publish_guard.py`.

**Always:** offline, numpy/pandas only · not public until notice (no push) · no live capital · refutation
ethos · respect+report the measured walls (TC⁰, abelian-only, bundle capacity envelope, seq_len≈256) ·
legible+tested code, one clean local commit · Turkish-first trilingual.
