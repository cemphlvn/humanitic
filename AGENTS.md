# AGENTS.md

Cross-tool agent identity for `ark` (the open standard; mirrors `CLAUDE.md`).

**ark** = Macro Strategy OS, powered by the Agentic Regime Kernel. Infrastructure to engineer/test/
repair/audit macro strategies — not a prediction bot. Operator loop: state → action → feedback → revision.

## Non-negotiable — Article 0 (the eternity clause)
1. `.local` is hard-gitignored. 2. `.local` is unpublishable. 3. Unpublishability is ungitignorable.
4. The removal of the unpublishability clause cannot even be proposed.
Never read/write/stage/publish/echo `.local`. Route every outbound artifact through
`foundation/privacy/publish_guard.py`.

## Rules
- Offline, numpy/pandas only. Not public until notice (no push/remotes). No live capital.
- Refutation ethos: try to break your own claims; a refutation is a result.
- Respect + report the measured walls (TC⁰, abelian-only, bundle capacity envelope, seq_len≈256).
- Legible code; tested increments; one clean local commit each.
- Trilingual: Turkish first, then English, then Chinese.

Full identity + run instructions: see [`CLAUDE.md`](CLAUDE.md). Plans: [`MASTERPLAN.md`](MASTERPLAN.md),
[`PLAN.md`](PLAN.md). Architecture: [`ARCHITECTURE.md`](ARCHITECTURE.md).
