# ── Article 0.1 — .local is HARD-gitignored (eternity clause; do not remove) ──
.local
.local/
*.local
**/.local/

# ── .labs — private learning sandbox (scratch experiments). Validated results graduate to
# foundation/ + a node; the messy iteration stays local. Not Article 0; just a working lab. ──
.labs
.labs/
**/.labs/

# python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# os
.DS_Store
