# ark — Macro Strategy OS · powered by the Agentic Regime Kernel

> **TR** — Makro Strateji İşletim Sistemi: makro stratejileri *ifade etmek, sınamak, zorlamak,
> dağıtmak ve revize etmek* için programlanabilir bir zemin; bir **Ajan-Rejim Çekirdeği**
> (Agentic Regime Kernel) ile çalışır. Bir tahmin botu değil — bir altyapı.
> *(Türkçe önce gelir: İngilizce ile Çince arasındaki köprü.)*
>
> **EN** — A programmable substrate to *express, test, stress, deploy, and revise* macro
> strategies; powered by an **Agentic Regime Kernel**. Not a prediction bot — infrastructure.
>
> **ZH** — 一个可编程的底座，用于*表达、回测、压力测试、部署与修正*宏观策略；由**智能体式
> 市场机制内核**（Agentic Regime Kernel）驱动。这不是预测机器人，而是基础设施。

We do **not** ask the model to predict prices. We ask it to **engineer, test, repair, and audit
strategies** inside a deployment-consistent finance architecture.

## The five layers

| Layer | What | Lineage anchor |
|---|---|---|
| **Operator** | GLM-style agentic engineering — builder/operator; carries depth past the TC⁰ ceiling **and** the ordered non-abelian pipeline | the agent loop |
| **Regime** | HD/VSA (FHRR torus kernel) + JEPA latent regime-transition prediction | gen-1→gen-5 substrate |
| **Execution** | FinRL-X weight-vector contract `w_t` — the stable interface | `kernel/contracts.py` |
| **Commons** | **HUMANITIK** — local knowledge index + failure database + attribution & reward | the moat |
| **Privacy** | `.local` — the **eternity clause** (Article 0) | secure-by-architecture |

## The four common codes (the irreducible floor)

`operator loop` (state→action→feedback→revision) · `the five VSA primitives` ·
`the weight-vector contract` · `the cost-aware walk-forward falsifier`.

## Run it (offline, this Mac, numpy only)

```bash
PYTHONPATH=. python3 -m foundation.selftest     # the floor: VSA laws + backtest + guards
PYTHONPATH=. python3 tests/run_all.py           # the test suites
```

## Article 0 — read first

`.local` is hard-gitignored **and** unpublishable; that guarantee cannot be hidden or even
proposed away. See [`CONTRIBUTING.md`](CONTRIBUTING.md) and
[`foundation/constitution/00-eternity-clause.md`](foundation/constitution/00-eternity-clause.md).

## Map

[`MASTERPLAN.md`](MASTERPLAN.md) · [`PLAN.md`](PLAN.md) · [`ARCHITECTURE.md`](ARCHITECTURE.md) ·
[`VAULTS.md`](VAULTS.md) · [`FORUM.md`](FORUM.md) · [`foundation/nodes/`](foundation/nodes) ·
[`foundation/index/`](foundation/index)

**Status:** local-only. **Not public until notice.** No push, no remote, no live trading.
